remote_command:
  description: Выполнение команды по номеру / Execute command by code
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
    command_id:
      description: Номер команды / Command code
      example: 1
      selector:
        number:
          min: 0
          max: 99999999
          mode: box
lock:
  description: Постановка на охрану / Lock vehicle
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
unlock:
  description: Снятие с охраны / Unlock vehicle
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
start_engine:
  description: Запуск двигателя / Start engine
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
stop_engine:
  description: Остановка двигателя / Stop engine
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_tracking:
  description: Включение отслеживания / Enable tracking
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
disable_tracking:
  description: Отключение отслеживания / Enable tracking
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_active_security:
  description: Включение активной защиты / Enable active security
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
disable_active_security:
  description: Отключение активной защиты / Disable active security
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_on_coolant_heater:
  description: Включение предпускового подогрева / Enable coolant heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_off_coolant_heater:
  description: Отключение предпускового подогрева / Disable coolant heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_on_ext_channel:
  description: Включение дополнительного канала (внешнее устройство) / Enable extra
    channel (external device)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_off_ext_channel:
  description: Отключение дополнительного канала (внешнее устройство) / Disable extra
    channel (external device)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_service_mode:
  description: Включение сервисного режима ("режим ТО") / Enable service mode
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
disable_service_mode:
  description: Отключение сервисного режима ("режим ТО") / Disable service mode
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_status_output:
  description: Включение статусного выхода (нештатный иммобилайзер) / Enable status
    output (external immobilizer)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
disable_status_output:
  description: Выключение статусного выхода (нештатный иммобилайзер) / Disable status
    output (external immobilizer)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
trigger_horn:
  description: Издать звук сиреной / Trigger alarm sound
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
trigger_light:
  description: Включить освещение фарами / Trigger exterior lighting
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
trigger_trunk:
  description: Открыть багажник / Open trunk
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
check:
  description: Отправить команду CHECK / Send CHECK command
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
erase_dtc:
  description: Очистка кодов DTC / Erase DTC codes
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
read_dtc:
  description: Считывание кодов DTC / Read DTC codes
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
additional_command_1:
  description: Дополнительная команда №1 / Additional command no. 1
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
additional_command_2:
  description: Дополнительная команда №2 / Additional command no. 2
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_connection:
  description: Форсировать передачу данных / Force data transmission
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
disable_connection:
  description: Приостановить передачу данных / Pause data transmission
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_on_block_heater:
  description: Включить предпусковой подогреватель / Turn On Block Heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
turn_off_block_heater:
  description: Выключить предпусковой подогреватель / Turn Off Block Heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_disable_service_mode:
  description: (NAV12) Отключить сервисный режим / Disable Service Mode
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_enable_service_mode:
  description: (NAV12) Включить сервисный режим / Enable Service Mode
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_turn_off_block_heater:
  description: (NAV12) Выключить предпусковой подогреватель / Turn Off Block Heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_turn_on_block_heater:
  description: (NAV12) Включить предпусковой подогреватель / Turn On Block Heater
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_reset_errors:
  description: (NAV12) Сброс ошибок / Reset Errors
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_enable_status_output:
  description: (NAV12) Включение статусного выхода / Enable Status Output
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
nav12_disable_status_output:
  description: (NAV12) Отключение статусного выхода / Disable Status Output
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_set_temperature:
  description: Установить целевую температуру салона / Climate Set Temperature
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
    climate_temp:
      description: Target temperature to maintain within the interior
      example: 18
      selector:
        number:
          min: 16
          max: 33
          step: 1
          unit_of_measurement: °C
climate_seat_heat_turn_on:
  description: Включить (понизить) обогрев сидений / Climate Seat Heat Turn On (Lower)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_seat_heat_turn_off:
  description: Выключить обогрев сидений / Climate Seat Heat Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_seat_vent_turn_on:
  description: Включить (понизить) вентиляцию сидений / Climate Seat Vent Turn On (Lower)
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_seat_vent_turn_off:
  description: Выключить вентиляцию сидений / Climate Seat Vent Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_glass_heat_turn_on:
  description: Включить обогрев стёкол и зеркал / Climate Glass Heat Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_glass_heat_turn_off:
  description: Выключить обогрев стёкол и зеркал / Climate Glass Heat Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_steering_heat_turn_on:
  description: Включить обогрев рулевого управления / Climate Steering Heat Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_steering_heat_turn_off:
  description: Выключить обогрев рулевого управления / Climate Steering Heat Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_ac_turn_on:
  description: Включить кондиционер / Climate Ac Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_ac_turn_off:
  description: Выключить кондиционер / Climate Ac Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_sys_turn_on:
  description: Включить климатическую систему / Climate Sys Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_sys_turn_off:
  description: Выключить климатическую систему / Climate Sys Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_defroster_turn_on:
  description: Включить отопитель лобового стекла / Climate Defroster Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_defroster_turn_off:
  description: Выключить отопитель лобового стекла / Climate Defroster Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_mode_comfort:
  description: Включить режим "комфорт" / Climate Mode Comfort
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_mode_vent:
  description: Включить режим вентиляции салона / Climate Mode Vent
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_battery_heat_turn_on:
  description: Включить обогрев аккумулятора / Climate Battery Heat Turn On
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
climate_battery_heat_turn_off:
  description: Выключить обогрев аккумулятора / Climate Battery Heat Turn Off
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
enable_stealth_mode:
  description: Включить режим Stealth / Enable Stealth Mode
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
stay_home_propion:
  description: Stay Home Propion / Stay Home Propion
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
ps_call:
  description: Ps Call / Ps Call
  fields:
    device_id:
      required: true
      description: Home Assistant device within the `pandora_cas` integration domain
      selector:
        device:
          integration: pandora_cas
